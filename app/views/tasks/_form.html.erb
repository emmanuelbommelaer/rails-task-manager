<%= form_with model: @task do |t| %>
<div>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <% unless t.object.new_record? %>
    <%= t.label :completed %>
    <%= t.check_box :completed %>
  <% end %>
  <%= t.submit %>
</div>
<% end %>
