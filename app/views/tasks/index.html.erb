<h1>My Tasks</h1>
  <% @tasks.each do |t| %>
  <p>
      <% if t.completed%>
        <i class="fa-regular fa-square-check"></i>
      <% else %>
        <i class="fa-regular fa-square"></i>
      <% end %>
    <%= link_to t.title, task_path(t) %>
  </p>
<% end %>
<p><%= link_to "Create task", new_task_path %></p>
